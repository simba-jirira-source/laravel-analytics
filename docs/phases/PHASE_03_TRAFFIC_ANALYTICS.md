# Phase 3 — Traffic Analytics

Implement request/page-view tracking with enable/disable controls, ignored paths/routes/methods, dashboard self-exclusion, response status, duration, safe referrer/user-agent collection, and tests. Never persist request bodies, cookies, secrets, tokens, auth headers, or session payloads. Run quality gates and stop.
