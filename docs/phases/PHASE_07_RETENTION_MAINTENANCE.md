# Phase 7 — Retention and Maintenance

Implement `analytics:prune`, configurable retention, idempotent pruning, tests, and scheduling documentation. Do not silently register destructive schedules. Run quality gates and stop.
