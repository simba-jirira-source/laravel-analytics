# Phase 6 — IP Banning

Implement opt-in exact IPv4/IPv6 bans, expiry, reason, middleware, ban/unban services, trusted-proxy-safe client IP use, CLI recovery commands, authorization-ready design, and tests. Feature remains disabled by default. Do not add CIDR unless explicitly justified and fully tested. Stop after quality gates.
