# Phase 2 — Configuration and Persistence

Implement `config/analytics.php`, publishable config, migrations, models, casts, indexes, and database tests. Use privacy-conscious defaults. Do not implement request tracking or dashboard functionality yet. Run quality gates and stop.
